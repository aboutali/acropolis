# Module 08 — src/08-southslope.js
Signature: `window.buildSouthSlope = function (THREE, mats, H) { ...; return group; };`  Group at (0,0,0). Budget ≈ 12k tris.

- Rock apron: `var apron = H.makeRockOutcrop(260, 120, 10, 3); apron.position.set(-60, -45, 85); apron.rotation.x = 0.31;` (slopes down toward +z).
- Theatre of Dionysus, a sub-group at (-6, -46, 96), open side toward +z: 24 cavea rings: for i in 0..23, r = 22 + i*1.05, `new THREE.CylinderGeometry(r, r, 0.6, 48, 1, true, -1.75, 3.5)` (a 200° arc), mesh `mats.marbleShadowed`, `rotation.y = Math.PI` so the opening faces +z, position y = i*0.6; castShadow true, receiveShadow true. Orchestra: `CylinderGeometry(19, 19, 0.3, 40)` `mats.marble` at y = -0.15. Skene wall: `BoxGeometry(30, 6, 3)` `mats.marbleWorn` at (0, 3, 20).
- Odeon of Herodes Atticus, a sub-group at (-140, -52, 72): 18 rings with r = 14 + i*1.33, height step 0.8, `CylinderGeometry(r, r, 0.8, 48, 1, true, -1.571, 3.142)` (180°), `rotation.y = Math.PI`, `mats.marbleShadowed`. Half-disc orchestra `CylinderGeometry(12, 12, 0.3, 32, 1, false, -1.571, 3.142)` rotated the same way at y = -0.15. Stage wall `BoxGeometry(76, 22, 3)` `mats.rock` at (0, 11, 22). Niches: 3 tiers × 8: dark boxes `BoxGeometry(3, 5, 0.8)` `mats.marbleShadowed` at z = 22 - 1.2, x evenly in [-33, 33], y = 4, 10, 16 (one H.instance); arches: `TorusGeometry(1.5, 0.3, 8, 16, Math.PI)` `mats.rock` centred on top of each niche at z = 22 - 1.2 (rotation none: the half-torus opens downward by default when rotated PI about z if needed — make sure the arc is above the niche), one H.instance of 24.
